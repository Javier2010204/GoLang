package main

import "fmt"

// O(n^2) => el tiempo de ejecucion va aumentando cuadraticamente segun la cantidad de elementos

func main() {
	
	var arr [5]string =["Lunes", "Martes", "Miercoles", "Jueves", "Viernes"]
	var arr [3]string =["Dia", "Tarde", "Noche"]

	for i := 0; i < len(arr); i++ {
		for x := 0; x < len(arr2); i++ {
			fmt.Println(arr[x])
		}
	}

}